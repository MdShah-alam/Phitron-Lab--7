#include<stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    int a;
    scanf("%d",&a);
    int ar[a];

    for(int i=0; i<a; i++)
    {
        scanf("%d",&ar[i]);
    }

    int b;
    scanf("%d",&b);
    int br[b];

    for(int i=0; i<b; i++)
    {
        scanf("%d",&br[i]);
    }
    for(int i=1;i<=n;i++)
    {
        for(int k=0,j=0;k<a,j<)
        if(ar[i]==n)
        {
            printf("I become the guy.");
            return 0;
        }

        if(br[j]==n)
        {
            printf("I become the guy.");
            return 0;
        }
    }
    printf("Oh, my keyboard!");
    return 0;
}
